# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> Status: Built and green (tsc + lint + build + SSR smoke-tested). Data layer, homepage sections, and
> the `/admin` dashboard (auth gate + CRUD for projects/experiences/skills/education/settings, dnd-kit
> reorder, JSON export/import/reset) are all implemented. Remaining: drop `public/profile.jpg`, set
> `VITE_ADMIN_PASSWORD` in `.env.local` (falls back to `admin2025`), and optional standalone routes
> (`/projects`, `/experience`, `/contact`).
> `AGENTS.md` (generated by TanStack Intent) holds per-library skill mappings.

## Project

Personal portfolio for Abdoulaye Kemogoha COULIBALY (Full Stack dev, Abidjan). Single-page portfolio
plus a **local-only admin dashboard** that edits all site content without a backend. UI copy is in
French; code, identifiers, and comments are in English.

## Stack

- **React 19** + **TanStack Start** + **Vite 8** (file-based routing via `@tanstack/react-router`)
- **React Compiler** enabled (`babel-plugin-react-compiler`) — see memoization note below
- **TanStack Query** (`@tanstack/react-query`); integration in `src/integrations/tanstack-query/`
- **Tailwind CSS v4** (`@tailwindcss/vite`) + **shadcn/ui** (style `new-york`, base color `zinc`)
- **Framer Motion** for all animation
- **TypeScript strict** — no `any`
- **TanStack Form + Zod v4** for forms/validation
- **@dnd-kit** for admin drag-and-drop reordering
- **lucide-react** icons; Google Fonts: Syne (display), Inter (body), JetBrains Mono (code)

## Commands

Package manager is **pnpm**. (npm/yarn will not respect the workspace config below.)

| Command | What |
|---------|------|
| `pnpm dev` | Dev server on **port 3000** |
| `pnpm build` | Production build (client + SSR) |
| `pnpm preview` | Serve the production build |
| `pnpm test` | Run Vitest once (`vitest run`) |
| `pnpm test <path>` / `pnpm exec vitest run <path>` | Single test file/pattern |
| `pnpm lint` / `pnpm format` | ESLint / Prettier+ESLint --fix |
| `pnpm generate-routes` | Regenerate `src/routeTree.gen.ts` (`tsr generate`) — usually automatic via Vite |

Add shadcn components with `pnpm dlx shadcn@latest add <component>` (writes to `src/components/ui/`).

### Import alias — important

Imports use **`#/`** (Node subpath imports), NOT `@/`. e.g. `import { Button } from '#/components/ui/button'`,
`import { cn } from '#/lib/utils'`. Maps to `./src/*` (see `package.json` `imports` + `components.json` aliases).

### Project layout — feature-based

Code is organised by **feature**, not by file type. Each feature owns its components, hooks, and
logic; a shared kernel holds cross-cutting pieces.

```
src/
├── features/
│   ├── data/         # the data domain — types.ts, schemas.ts, defaults.ts, storage.ts,
│   │                 #   PortfolioDataContext.tsx, usePortfolioData.ts (imported by both other features)
│   ├── portfolio/    # public site — components/ (sections + Navbar/Footer/ScrollProgress + Project/SkillCard)
│   │                 #   and hooks/ (useTypingEffect, useActiveSection, useScrollProgress)
│   └── admin/        # dashboard — components/ (AdminLayout, AdminAuthGate, form modals, dnd list, inputs)
├── components/
│   ├── ui/           # shadcn primitives (keep here — components.json points at it)
│   ├── shared/       # feature-agnostic presentational: Reveal, SectionHeader, CountUp
│   └── layout/       # app-shell: ThemeProvider, ThemeToggle
├── hooks/            # cross-cutting: useScrollAnimation
├── lib/              # animations/, utils.ts (cn), utils/constants.ts
└── routes/           # thin TanStack route files that compose feature components
```

Conventions: intra-feature and cross-feature imports use **specific file paths** (e.g.
`#/features/data/types`, `#/features/data/usePortfolioData`) — there are no barrel `index.ts` files
(avoids circular deps and `import/no-duplicates` churn). Files inside `features/data/` import each
other via `#/features/data/<file>` too. Routes are the only place that "compose" multiple features.

### pnpm gotcha (already fixed, don't undo)

`pnpm-workspace.yaml` sets `verifyDepsBeforeRun: false` and lists `unrs-resolver` under
`onlyBuiltDependencies`. Without this, pnpm's ignored-build guard makes every `pnpm <script>` exit
non-zero (it also silently aborts `shadcn add`). If you hit `ERR_PNPM_IGNORED_BUILDS`, remove
`node_modules/.modules.yaml` and re-run `pnpm install` rather than reverting the config.

## Architecture — the parts that matter

### Data flow is the core design (read this first)

All editable content lives in **one** `PortfolioData` object persisted to `localStorage` under key
`portfolio_data`. There is no server. The flow is strictly one-directional:

```
defaults.ts (seed) ──► storage.ts (load/save/import/export) ──► PortfolioDataContext (Context + reducer)
                                                                          │
                            usePortfolioData() ◄──────────────────────────┘
                                   │
                  every section + every admin page reads here
```

Hard rules that the whole codebase depends on:

- **Components NEVER import `defaults.ts` directly.** They read content only through
  `usePortfolioData()`. `defaults.ts` is the first-run seed and the reset target — nothing else.
- **Every mutation goes through the reducer** via a named `DataAction` command
  (`UPDATE_PERSONAL_INFO`, `ADD_PROJECT`, `DELETE_PROJECT`, `REORDER_PROJECTS`, `IMPORT_DATA`,
  `RESET_TO_DEFAULTS`, …). No component mutates state directly.
- **Each dispatch auto-saves to localStorage** (and bumps `lastUpdated`). Saving is a side effect of
  the reducer/provider, not the caller's job.
- **Import JSON must be Zod-validated before it is applied.** Export is a plain JSON download.

Single source of truth for shapes: TypeScript interfaces in `src/features/data/types.ts` and Zod
schemas in `src/features/data/schemas.ts` must stay aligned — `schemas.ts` enforces this at compile
time via the `SchemaTypeChecks` `Equal<>` tuple (a drift becomes a `tsc` error). `PortfolioData` =
`{ personalInfo, skills, projects, experiences, education, lastUpdated }`. (`src/lib/utils.ts` with
`cn()` exists from shadcn.)

### Admin access

`/admin/*` is gated by a password dialog comparing against `import.meta.env.VITE_ADMIN_PASSWORD`
(set in `.env.local`, never committed). A valid session is stored in `sessionStorage['admin_auth']`
(session, not local). Exclude `/admin` from sitemap/SEO.

### Layout / routing

File-based routes live in `src/routes/`; `src/routeTree.gen.ts` is **generated** (don't edit).
`src/routes/__root.tsx` is the root layout — wrap the app there in `PortfolioDataProvider` +
ThemeProvider (the TanStack Query provider is already wired via `src/integrations/tanstack-query/`).
Global CSS is `src/styles.css` (Tailwind v4, `@import`-based — no `tailwind.config.ts`).

Target routes: portfolio (`index`, `projects`, `experience`, `contact`) render section components;
`admin/*` render CRUD pages inside `AdminLayout` (separate dark admin theme: bg `#0F172A`, sidebar
`#1E293B`).

## Conventions

- **SOLID / Container-Presenter**: presentational components (`ProjectCard`, `SkillBadge`) only
  render; orchestration and data access live in containers/hooks. Extend via props
  (`variant`/`size`/`as`), don't fork components.
- Reusable logic → custom hooks. Cross-cutting hooks live in `src/hooks/` (`useScrollAnimation`);
  feature hooks live with their feature (`src/features/portfolio/hooks/`, `src/features/data/usePortfolioData.ts`).
- No magic numbers — named constants in `src/lib/utils/constants.ts`. Comments explain *why* only.
- Animation: shared variants (`fadeUpVariants`, `staggerContainerVariants`) and `useScrollAnimation`
  (`useInView`, `once: true`). **Respect `prefers-reduced-motion` everywhere.**
- Theme: dark by default, class-strategy toggle. Accents blue `#3B82F6` / teal `#14B8A6`.

## Quality bar (from spec)

Zero `any`; types ⇄ Zod schemas aligned; all mutations via reducer with auto-save; import validated;
reset requires confirmation; lazy images; WCAG AA contrast + full keyboard nav + `aria-label` on
icon-only controls; per-page meta tags and a JSON-LD Person schema generated from `personalInfo`.

**React 19**: do NOT hand-write `useMemo`/`useCallback`/`React.memo` — the React Compiler handles
memoization. Write plain components; only reach for manual memoization if profiling proves a need.
